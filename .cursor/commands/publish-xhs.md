请将下面的markdown文件内容整理并发布到小红书，一定严格按照下面的步骤执行。

## 1. 将 markdown 文件转换为图片

首先使用 `python3 ./xhs/markdown2png.py --md_file_path <markdown文件绝对路径>` 将 markdown 文件转换为图片。

## 2. 使用 xiaohongshu-mcp 工具辅助完成发布流程

使用 `xiaohongshu-mcp` 工具中的 `publish_content` 工具辅助完成发布流程，确保图片与代码正常显示，内容整体美观有条理。

具体要求如下：

1. 标题格式：「【每日一题】题目名称」，例如「【每日一题】1161. 最大层内元素和」
2. 正文内容：对题目进行总结，并给出相关的思考，内容通俗易懂，利于初学者入门，但也要有深度剖析，突出解题亮点或技巧，适合小红书平台风格。
3. 封面图片链接：为 /home/zql/code/xhs/images/今天日期/ 目录下的所有图片
4. 标签建议包含：#每日一题 #LeetCode #算法 #数据结构 #刷题
5. 用 xiaohongshu-mcp 工具中的 `publish_content` 工具辅助完成发布流程，确保图片与代码正常显示，内容整体美观有条理。
6. 正文中的标题不要加 * 符号，可以添加一些 emoji 符号

请严格按照上述要求生成并发布内容。

只有当这一步发布成功后，才能进行下一步。

## 3. 将题目信息写入小红书表格中

完成步骤2后，使用 `python3 ./xhs/write_xhs_table.py --title "【每日一题】题目名称"` 将题目信息写入小红书表格中。
